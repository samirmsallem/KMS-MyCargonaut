{
  "name": "web",
  "version": "0.0.0",
  "scripts": {
    "heroku-postbuild": "cd client && npm install && cd ../server && npm i && npm install mongoose && npm i --save-dev @types/node && cd ../client && npm run build:prod && cd ../server && npm run build",
    "start": "cd server && npm run start"
  }
}
